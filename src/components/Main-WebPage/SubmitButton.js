import React from "react";

const SubmitButton = () => {
  // must create a function that takes input from searchBox onSubmit
  return (
    <div>
      <button type="button" class="btn" style={{ backgroundColor: "#DA5B00" }}>
        Summarize
      </button>
    </div>
  );
};

export default SubmitButton;
